const router = require("express").Router();

// Offer generation is handled via /api/application/:applicationId/offer (applicationRoutes.js)
// using applicationController.generateOffer with full usage tracking.
// This route file is intentionally empty to avoid duplicate endpoints.

module.exports = router;
